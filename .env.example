# cp .env.example .env 후 값 채우기. .env 는 커밋 금지.
DB_URL=jdbc:postgresql://localhost:5432/site
DB_USERNAME=site
DB_PASSWORD=site

ADMIN_USERNAME=sungbin
# BCrypt 해시. README 의 생성 방법 참고. $ 가 들어가므로 따옴표로 감쌀 것.
ADMIN_PASSWORD_HASH=
