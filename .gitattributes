# 줄바꿈은 저장소 안에서 항상 LF 로 통일한다.
# Windows 에서 커밋해도 Linux 러너에서 gradlew 가 깨지지 않게 하기 위함.
* text=auto eol=lf

# Windows 전용 스크립트만 CRLF 유지
*.bat  text eol=crlf
*.cmd  text eol=crlf

# 바이너리로 취급 (줄바꿈 변환 금지)
*.jar  binary
*.png  binary
*.jpg  binary
*.gif  binary
*.ico  binary
*.woff binary
*.woff2 binary
